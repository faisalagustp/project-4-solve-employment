
<%= render 'partials/employer-profile' %>

<div class="container-fluid">
<%if @employer.user.id == current_user.id%>
<%= link_to 'Edit', edit_user_registration_path, :class => "btn btn-primary" %>
<%end%>
<%= link_to 'Back', jobs_path, :class => "btn btn-primary" %>
</div>
